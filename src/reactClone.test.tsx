import ReactClone from "./setup";

describe("ReactClone", () => {
    /* it("should create a ReactClonElement with the specified type, props, and children", () => {
        const type = "div";
        const props = { id: "test" };
        const children = [{ id: 1, type: "p", props: {}, children: [] }];
        const element = ReactClone.createElement(type, props, children[0]);
    
        expect(element.type).toBe(type);
        expect(element.props).toBe(props);
        expect(element.children).toBe(children);
    });
    it("should create a ReactClonElement with the specified type, props, and children", () => {
        const type = "div";
        const props = { id: "test" };
        const children = [{ id: 1, type: "p", props: {}, children: [] }];
        const element = ReactClone.createElement(type, props, children[0]);
    
        expect(element.type).toBe(type);
        expect(element.props).toBe(props);
        expect(element.children).toBe(children);
    });    */ 
    }
);