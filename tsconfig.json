{
  "compilerOptions": {
    "target": "es5",                    // Compila a ES5 para una amplia compatibilidad con navegadores
    "module": "commonjs",               // Usa CommonJS para módulos
    "lib": ["dom", "es6", "es2017", "esnext"], // Incluye librerías para el DOM y características de ECMAScript
    "jsx": "preserve",                  // Preserva el JSX para que Babel pueda transpilarlo
    "strict": true,                     // Habilita todas las opciones estrictas de tipo
    "esModuleInterop": true,            // Permite la interoperabilidad de módulos ES6 con CommonJS
    "skipLibCheck": true,               // Omite la verificación de tipos en las declaraciones de las librerías
    "forceConsistentCasingInFileNames": true, // Asegura que la capitalización sea consistente en las importaciones
    "outDir": "./dist",                 // Directorio para los archivos transpilados
    "declaration": true,                // Genera archivos '.d.ts' para TypeScript
    "declarationDir": "./dist"          // Directorio para los archivos de declaración
  },
  "include": [
    "src/**/*"                          // Incluye todos los archivos en src
  ],
  "exclude": [
    "node_modules",                     // Excluye node_modules
    "**/*.test.ts"                      // Excluye archivos de prueba
  ]
}
